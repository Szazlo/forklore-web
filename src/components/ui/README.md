# radix and own components

We use components from other libraries like radix ui. This is because we make use of components that MUI does not provide yet, like carousels. We also have our own components.

To distinguish from radix components, we use PascalCase for our components.
